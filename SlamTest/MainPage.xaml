<Page
    x:Class="SlamTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlamTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Name="SerialReadTextBlock" Grid.Column="0"></TextBlock>
            </Grid>
            <ComboBox Grid.Row="1" Name="SerialList" Background="Bisque"></ComboBox>

            <Grid  Grid.Row="2" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Name="StartSerialButton" IsEnabled="False" Click="StartSerialButton_OnClick">Start Serial!</Button>
                <Button Grid.Column="1" Name="StopSerialButton" IsEnabled="False" Click="StopSerialButton_OnClick">Start Serial!</Button>

            </Grid>
            
            <TextBlock Grid.Row="3" Name="SerialInfoTextBlock" TextWrapping="Wrap"  FontSize="32" VerticalAlignment="Stretch" >Select Serial Port</TextBlock>
        </Grid>

    </Grid>
<Page.TopAppBar>
        <CommandBar>
            <AppBarButton Name="RefreshButton" Icon="Refresh" Label="Refresh Serial Devices" Click="RefreshButton_OnClick"></AppBarButton>


    </CommandBar>
</Page.TopAppBar>

</Page>
