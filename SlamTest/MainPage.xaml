<Page
    x:Class="SlamTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlamTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Name="SerialReadTextBlock" Grid.Column="0"></TextBlock>
            </Grid>
            

            <Grid  Grid.Row="2" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
            </Grid>
            
            <TextBlock Grid.Row="3" Name="SerialInfoTextBlock" TextWrapping="Wrap"  FontSize="32" VerticalAlignment="Stretch" >Select Serial Port</TextBlock>
        </Grid>

    </Grid>
<Page.TopAppBar>
        <CommandBar>
            <AppBarButton Name="StartStopButton" Icon="Play" IsEnabled="False" ToolTipService.ToolTip="Start Serial Read" Click="StartStopSerialButton_OnClick"></AppBarButton>

            <AppBarButton Name="RefreshButton" Icon="Refresh" Label="Refresh Serial Devices" ToolTipService.ToolTip="Stop Serial Read" Click="RefreshButton_OnClick"></AppBarButton>
            
            <!--<CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Bluetooth settings">
                    <Hyperlink NavigateUri="ms-settings:bluetooth">
                        <Run Text="Settings" />
                    </Hyperlink>
                </AppBarButton>
            </CommandBar.SecondaryCommands>-->
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="8" FontSize="18">Serial Device:</TextBlock>
                <ComboBox Margin="7" MinWidth="150"  Name="SerialList" Background="Bisque"></ComboBox>
                </StackPanel>
            </CommandBar.Content>


    </CommandBar>
</Page.TopAppBar>

</Page>
